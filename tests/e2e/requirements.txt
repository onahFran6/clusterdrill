# Browser-level E2E dependencies - deliberately separate from
# web/requirements-dev.txt (never installed by that file's `-r`, and never
# needed for the fast, hermetic web/tests or clusterdrill/tests suites).
# This suite drives a real Chromium browser against a real, disposable
# Minikube profile - install only when actually running it:
#   pip install -r tests/e2e/requirements.txt && playwright install chromium
-r ../../web/requirements-dev.txt
playwright~=1.62
pytest-playwright~=0.9
